<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!--스프링 MVC 설정을 어노테이션 기반으로 처리하겠다는 의미
        스프링 MVC의 여러 객체들을 자동으로 스프링의 빈으로 등록하겠다.-->
    <mvc:annotation-driven></mvc:annotation-driven>

    <!--webapp 디렉토리 하위 resources 디렉토리 (정적 파일들의 저장소)의 위치 등록 -->
    <mvc:resources mapping="/resources/**" location="/resources/"></mvc:resources>

    <!--InternalResourceViewResolver : 스프링 MVC에서 제공하는 뷰(view)를 어떻게 결정할 것인지 설정을 담당하는 빈(객체)-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"></property> <!-- prefix : MVC에서 사용했던 WEB-INF 경로 -->
        <property name="suffix" value=".jsp"></property> <!-- suffix : '.jsp'확장자 지정-->
    </bean>

    <!--base-package에 존재하는 어노테이션을 모두 등록하라-->
    <context:component-scan base-package="com.multicampus.springex1.controller"/>

    <!-- Formatter를 servlet-context.xml에 적용하여 스프링의 빈으로 등록해야 한다
        이 등록을 위해 FormattingConversionServiceFactoryBean 객체를 스프링의 빈으로 등록해야 한다
        등록 후 FormattingConversionServiceFactoryBean 객체 안에 LocalDateFormatter를 추가한다-->

    <bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
        <property name="formatters">
            <set>
                <bean class="com.multicampus.springex1.controller.formatter.LocalDateFormatter"/>
                <bean class="com.multicampus.springex1.controller.formatter.CheckboxFormatter"/>
            </set>
        </property>
    </bean>

    <!--conversionService라는 빈을 등록한 후 스프링MVC를 처리할때 MVC: annotiation-driven 설정을 반드시 해줘야 한다-->
    <mvc:annotation-driven  conversion-service="conversionService" />

</beans>